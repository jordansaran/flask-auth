services:
  flask-auth:
    container_name: flask-auth-api
    build: .
    command: flask run --host=0.0.0.0 --port=8000
    volumes:
      - .:/usr/src/app/
    ports:
      - 8000:8000
    env_file:
      - ./.env