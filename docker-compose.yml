version: '1.0'
services:
  nestle-api:
    container_name: pipeline-nestle-api
    build: .
    command: flask run --host=0.0.0.0 --port=8000
    volumes:
      - .:/usr/src/app/
    ports:
      - 8000:8000
    env_file:
      - ./.env